services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    environment:
      - VITE_RPPG_WS=ws://localhost:8000/ws/rppg
    ports:
      - "5173:5173"
    depends_on:
      - backend
